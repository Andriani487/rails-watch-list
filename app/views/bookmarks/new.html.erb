<h1>Add a bookmark</h1>

<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.association :movie, label: "Choose a movie" %>
  <%= f.input :comment %>
  <%= f.button :submit, "Add Bookmark" %>
<% end %>

<%= link_to 'Back to list', list_path(@list), class: "btn btn-secondary mt-2" %>
